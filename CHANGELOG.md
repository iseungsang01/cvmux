# 업데이트 내역

새 버전을 낼 때 맨 위에 항목을 더한다. 릴리스 본문은 이 파일의 해당 버전 항목에서
만들어진다 — 항목이 없으면 릴리스가 멈춘다.

## 0.1.9 — 2026-09-18

- **옆 에이전트와의 주고받기를 끝낼 수 있다.** 에이전트가 답을 `[완료]`(또는 `[DONE]`)로
  시작하면 cvmux가 그 답을 옆으로 넘기지 않고 사람에게 알린다. 넘기는 메시지 머리말에도
  이 규칙을 적어 두어, 설명서를 읽지 않은 에이전트도 안다
- **Codex에게 줄 설명서** [docs/codex.md](./docs/codex.md) — 설치 방법과 협업 규칙.
  Codex에게 "이 설명서대로 설치해 줘"라고 하면 스스로 훅을 걸고 규칙을 `~/.codex/AGENTS.md`에 넣는다

## 0.1.8 — 2026-09-18

**옆 에이전트에게 답 넘기기.** 화면을 나눠 Claude Code와 Codex를 나란히 띄우고
분할선 가운데의 ⇄를 누르면, 한쪽이 답을 끝낼 때마다 그 답이 옆 입력창에 들어가
보내진다. 누를 때마다 끔 → 오른쪽으로 → 왼쪽으로 → 양쪽.

- 넘기는 것은 에이전트가 훅으로 알려 준 마지막 답이다. 먼저 훅을 건다:
  `cvmux hooks setup`(Claude Code), `cvmux hooks setup --agent codex`(Codex — 그다음
  Codex 안에서 `/hooks`로 한 번 승인)
- 옆 칸이 셸 프롬프트면 넣지 않는다. 옆 에이전트가 일하는 중이거나 권한을 묻는
  중이면 끝날 때까지 기다린다
- 사람 입력 없이 10번 이어지면 스스로 꺼진다(`relay.maxAutoTurns`)
- 켜 둔 연결은 앱을 다시 켜도 남는다

**설치본의 `cvmux` 명령이 앱 창을 띄우던 문제를 고쳤다.** 세션 안에서 `cvmux`를
치면 PowerShell이 CLI 대신 `cvmux.exe`를 잡아 창만 앞으로 나왔고, 에이전트 훅도
그래서 헛돌았다(알림·이어서 띄우기 기록이 닿지 않았다). CLI는 이제 설치 폴더의
`bin\`에 따로 있고 Git Bash용도 함께 있다.

- `cvmux --version`이 0.1.0으로 고정돼 있던 것을 고쳤다

## 0.1.7 — 2026-09-18

**RAM을 절반 아래로 줄였다.** 세션 6개를 켜 둔 기준으로 앱이 쓰는 메모리가
약 530MB에서 약 215MB가 됐다(셸 제외, 작업 관리자의 "메모리" 기준).

- 세션마다 하나씩 띄우던 출력 중계 스레드를 하나로 합쳤다. main 프로세스 95 → 50MB
- 터미널을 기본으로 DOM 렌더러로 그린다. GPU 프로세스 330 → 70MB 안팎.
  예전처럼 WebGL로 그리려면 설정에 `"terminal": { "gpuRendering": true }` —
  박스 문자가 끊김 없이 이어지고 출력이 쏟아질 때 조금 가볍지만 메모리를 더 쓴다

## 0.1.6 — 2026-09-18

- 앱을 다시 켜도 이전 세션의 화면(스크롤백)을 되살리지 않는다. 배치·작업 디렉토리·
  이름·에이전트 연결만 돌아오고, 셸은 빈 화면에서 시작한다. 스크롤백을 저장하지
  않으니 저장 파일도 가벼워졌다

## 0.1.5 — 2026-09-18

- **복원한 세션은 볼 때 켠다.** 앱을 켜면 처음 보이는 세션만 셸을 띄우고, 나머지는
  "꺼져 있음"으로 기다리다 누르는 순간 켜진다. 늘 켜 둘 줄은 사이드바의 📌로 고정한다
- 셸이 끝났는데 사이드바에 "셸" 칩이 남아 있던 문제를 고쳤다
- 0.1.4 설치본이 창을 띄우지 못하던 문제를 고쳤다

## 0.1.4 — 2026-09-09

> 이 버전의 설치본은 창이 뜨지 않는다. 0.1.5 이상을 쓸 것.

- 설치 파일 98MB → 79MB. 쓰지 않는 런타임 파일(번역 53벌, WebGPU 컴파일러 등)을 덜어냈다
- 타이틀바의 "업데이트 준비됨"을 누르면 묻지도 않고 앱이 꺼지던 문제를 고쳤다.
  이제 실행 중인 세션 수를 보여 주고 묻는다

## 0.1.3 — 2026-08-05

- 탭을 끌어 순서를 바꾼다
- 워크스페이스를 옮기면 제어 소켓이 `workspace.selected` 이벤트로 알린다
- 뜨지 않는 설치본은 릴리스에 오르지 못하게 CI가 검사한다

## 0.1.2 — 2026-08-05

- 0.1.1 설치본이 켜자마자 죽던 문제를 고쳤다(자동 업데이트 모듈이 빠져 있었다)

## 0.1.1 — 2026-08-05

첫 릴리스.

- 사이드바 신호등(실행 중·대기·확인 필요), git 브랜치·리슨 포트 표시
- 분할 창, pane 안의 가로 탭, 여러 창, 트레이 상주
- 에이전트 알림과 알림함, Windows 토스트
- 세션 복원과 에이전트 대화 이어서 띄우기(`cvmux hooks setup`)
- `cvmux` CLI와 제어 소켓, 에이전트가 쓰는 사이드바(상태·진행률·로그·체크리스트)
- 내장 브라우저와 에이전트용 조작 명령
- 명령 팔레트, 전체 세션 검색, 설정 파일, 자동 업데이트
