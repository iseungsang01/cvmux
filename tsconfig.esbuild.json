{
  "//": [
    "esbuild가 @shared/* · @core/* 를 풀 때 쓰는 설정.",
    "",
    "테스트와 CLI 번들마다 --alias 를 늘어놓으면 새 모듈을 만들 때마다 여러",
    "스크립트를 함께 고쳐야 하고, 하나를 빠뜨리면 그 테스트만 조용히 깨진다.",
    "경로를 한곳에 적어 두면 그런 일이 없다."
  ],
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@shared/*": ["./src/shared/*"],
      "@core/*": ["./src/core/*"]
    }
  }
}
