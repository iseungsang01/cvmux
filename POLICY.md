# cvmux 동작 정책 (Behavior Policy)

cvmux는 Windows용 터미널 워크스페이스 매니저다. 여러 셸 세션을 동시에 돌리고, 왼쪽
사이드바에서 각 세션이 지금 무엇을 하고 있는지 한눈에 파악하는 것이 존재 이유다.

이 문서는 **엣지 케이스를 만났을 때 cvmux가 어떻게 행동해야 하는가**를 규정한다.
참조 구현은 [cmux](https://github.com/manaflow-ai/cmux) (macOS, Swift + libghostty)이며,
cmux가 명시적으로 정의한 동작은 그대로 따르고, macOS 고유 동작은 Windows 등가물로 옮긴다.

각 규칙은 `P<영역>-<번호>` ID를 가진다. 구현 코드에는 해당 ID를 주석으로 남겨
정책과 구현을 상호 추적 가능하게 한다.

---

## 0. 설계 원칙

| # | 원칙 | 의미 |
|---|------|------|
| 0-1 | **세션은 절대 조용히 사라지지 않는다** | 셸이 죽어도 사이드바 항목은 남고, 종료 코드를 표시한다. 사용자가 명시적으로 닫아야 없어진다. |
| 0-2 | **출력은 절대 손실되지 않는다** | 백프레셔, 리사이즈, 창 숨김, 렌더러 재시작 어디서도 바이트가 유실되면 안 된다. |
| 0-3 | **추측은 표시하되 단정하지 않는다** | 상태 감지는 휴리스틱이다. 확실한 신호(OSC/프로세스 종료)와 추측(유휴 타이머)을 시각적으로 구분한다. |
| 0-4 | **터미널이 우선권을 갖는다** | 키 입력은 기본적으로 셸에 전달한다. 앱 단축키는 충돌하지 않는 것만 가로챈다. |
| 0-5 | **실패는 세션 단위로 격리된다** | 한 세션의 PTY 크래시가 다른 세션이나 앱 전체를 죽이면 안 된다. |

---

## P1. 세션 생명주기 (Session Lifecycle)

| ID | 엣지 케이스 | cmux 동작 | cvmux 정책 | 구현 위치 |
|----|-------------|-----------|-----------|----------|
| P1-1 | 셸이 정상 종료 (`exit`) | 패널 유지, 종료 상태 표시 | 세션을 `exited` 상태로 전환. 사이드바에 `exit 0` 배지. 터미널 버퍼는 그대로 읽기 가능. 자동 제거 금지 | `pty-manager.ts`, `Sidebar.tsx` |
| P1-2 | 셸이 0이 아닌 코드로 종료 | 종료 코드 노출 | `exited` + 종료 코드를 빨간색으로 표시. `exit 1` | 동일 |
| P1-3 | 셸이 시그널/강제 종료됨 | — | `exited` + `signal N` 표시. 종료 코드가 `null`이면 신호 종료로 간주 | `pty-manager.ts` |
| P1-4 | 종료된 세션을 다시 쓰려 함 | 새 세션 생성 | 종료된 세션 패널에 "Enter를 눌러 재시작" 안내. 재시작 시 **같은 cwd, 같은 세션 ID**로 PTY만 새로 띄운다 | `pty-manager.ts` |
| P1-5 | 셸 실행 파일 자체가 없음 (pwsh 미설치 등) | — | PTY spawn 실패를 세션 생성 실패로 처리하되, 세션 항목은 만들고 오류 메시지를 터미널 영역에 렌더링. 앱 크래시 금지 | `pty-manager.ts` |
| P1-6 | 세션 닫기 요청 시 자식 프로세스가 살아있음 | — | **프로세스 트리 전체를 종료**한다. Windows에서 PTY만 닫으면 손자 프로세스(node, python 등)가 고아로 남는다. `taskkill /T /F /PID` 등가 처리 | `pty-manager.ts` |
| P1-7 | 마지막 세션을 닫음 | — | 빈 상태 화면을 보여준다. 앱은 종료하지 않는다 | `App.tsx` |
| P1-8 | 세션 수가 상한을 넘김 | — | 기본 상한 32개. 초과 시 생성 거부 + 사이드바 상단에 사유 표시 | `pty-manager.ts` |
| P1-9 | 존재하지 않는 세션 ID로 IPC 호출 | — | 조용히 무시하고 `false` 반환. 예외를 던져 렌더러를 죽이지 않는다 | `ipc.ts` |

---

## P2. PTY / ConPTY (Windows 고유)

Windows의 PTY는 ConPTY다. macOS의 `forkpty`와 동작이 다르며, 여기가 이식에서
가장 많이 깨지는 지점이다.

| ID | 엣지 케이스 | cvmux 정책 | 구현 위치 |
|----|-------------|-----------|----------|
| P2-1 | 터미널 크기가 0 또는 음수 | `cols/rows`를 최소 1로 클램프한 뒤 resize 호출. ConPTY는 0을 받으면 오류를 던진다 | `pty-manager.ts` |
| P2-2 | 창 리사이즈가 초당 수십 회 발생 | resize를 **60ms 디바운스**. ConPTY resize는 비싸고 연속 호출 시 화면이 깨진다 | `terminal-host.ts` |
| P2-3 | 숨겨진(비활성) 세션의 리사이즈 | 숨김 상태에서는 컨테이너 크기가 0이므로 **fit을 건너뛴다**. 활성화되는 순간 1회 fit + resize | `terminal-host.ts` |
| P2-4 | 리사이즈 직후 재랩(reflow) | ConPTY가 스크롤백을 다시 흘려보내므로 이를 정상 출력으로 취급. 상태 감지 타이머를 리사이즈 후 300ms 억제해 오탐 방지 | `session-state.ts` |
| P2-5 | 종료된 PTY에 write/resize | 무시. `EPIPE`/`Access denied` 예외를 삼킨다 | `pty-manager.ts` |
| P2-6 | ConPTY 미지원 환경 (Windows 10 1809 미만) | 시작 시 빌드 번호 확인. 미지원이면 명확한 안내 후 종료 | `main/index.ts` |
| P2-7 | 셸 프로세스가 즉시 죽음 (100ms 이내) | 재시작 루프 방지 — 자동 재시작하지 않고 종료 코드만 표시 | `pty-manager.ts` |

---

## P3. 스트림 무결성 · 인코딩

| ID | 엣지 케이스 | cvmux 정책 | 구현 위치 |
|----|-------------|-----------|----------|
| P3-1 | UTF-8 멀티바이트 문자가 청크 경계에서 잘림 | node-pty를 **문자열 모드**로 쓰되, 잘린 시퀀스는 xterm.js가 내부적으로 버퍼링하도록 원본 순서를 보존해 전달. 임의 가공(trim/split) 금지 | `pty-manager.ts` |
| P3-2 | ANSI 이스케이프가 청크 경계에서 잘림 | 상태 감지용 파싱은 **누적 버퍼**에서 수행. 청크 단위로 정규식을 돌리지 않는다 | `session-state.ts` |
| P3-3 | 한글 Windows 코드페이지(949) 출력 깨짐 | 세션 시작 시 PowerShell에 `[Console]::OutputEncoding = [Text.UTF8Encoding]::new()` 부트스트랩 주입. 사용자 프로필을 건드리지 않고 세션 한정 | `pty-manager.ts` |
| P3-4 | 초당 수 MB 대량 출력 (`cat` 대용량 파일) | IPC를 **16ms 간격으로 배칭**. 프레임당 1회 전송. 배치 버퍼 상한 256KB, 초과 시 즉시 flush | `pty-manager.ts` |
| P3-5 | 바이너리 데이터가 터미널에 쏟아짐 | 필터링하지 않는다(터미널의 정상 동작). 단 미리보기 텍스트에는 제어문자를 제거한 결과만 사용 | `session-state.ts` |
| P3-6 | 출력이 무한 루프로 계속됨 | 세션별 스크롤백 상한 기본 10,000줄 — 값은 설정이 정한다(`terminal.scrollback`, P22-2). 상태 판정은 화면 전체가 아니라 **커서가 놓인 줄 하나**만 들고 있으므로 출력이 아무리 흘러도 자라지 않는다 | `terminal-host.ts`, `ansi-parser.ts` |
| P3-7 | 런처가 `NO_COLOR`를 켠 채 cvmux를 실행 | 세션 환경에서 **`NO_COLOR`를 제거한다**. cvmux 세션은 트루컬러를 완전히 지원하는 새 터미널이고, 앱을 띄운 부모 프로세스의 색상 정책이 세션 안으로 새어들 이유가 없다. 실제로 다른 에이전트 CLI 안에서 cvmux를 띄우면 그 CLI가 심어둔 `NO_COLOR=1` 때문에 모든 하위 CLI가 흑백이 된다. 색을 원치 않으면 `CVMUX_NO_COLOR=1`로 명시한다 | `pty-manager.ts` |
| P3-8 | `TERM=dumb` 상속 | 마찬가지로 세션에서는 `xterm-256color`로 덮어쓴다. 터미널의 능력은 세션이 결정한다 | 동일 |
| P3-9 | 런처가 Electron 앱 | 세션 환경에서 **`ELECTRON_RUN_AS_NODE`를 제거한다**. P3-7과 같은 부류다 — 에이전트 CLI 안에서 cvmux를 띄우면 이 값이 그대로 흘러들고, 그러면 세션 안에서 실행한 Electron 앱이 창 대신 Node 스크립트로 뜬다. 실제로 이 프로젝트를 개발하다 겪었다 | 동일 |
| P3-9 | 셸이 작업 디렉토리·프롬프트 경계를 알려주지 않음 | 세션 시작 시 프로필의 `prompt`를 **감싸** OSC 7(작업 디렉토리)과 OSC 133(셸 통합)을 심는다. 프로필 파일은 건드리지 않는다. 원본 `prompt`를 가장 먼저 호출해 `$?`/`$LASTEXITCODE`를 온전히 넘긴다 — 순서가 반대면 실패를 색으로 알리는 테마가 전부 성공한 것처럼 보인다. `CVMUX_NO_SHELL_INTEGRATION=1`로 끌 수 있다 | `pty-manager.ts` |

---

## P4. 상태 감지 · 알림 (핵심)

cmux는 **명시적 신호**(OSC 9 / OSC 777 / OSC 99 / BEL, `cmux notify` CLI)와
**시각적 표시**(파란 링, 사이드바 배지)를 결합한다. cvmux도 동일한 계층 구조를 따른다.

### 신호 우선순위

```
1순위 (확실)  OSC 9 / OSC 777 / OSC 99 / BEL   → 에이전트가 명시적으로 알림
2순위 (확실)  프로세스 종료                      → exited
3순위 (강함)  OSC 133 셸 통합 프롬프트 마커       → idle 확정
4순위 (추측)  프롬프트 정규식 + 유휴 타이머        → idle 추정
5순위 (추측)  출력 흐름 유무                     → busy
```

| ID | 엣지 케이스 | cvmux 정책 | 구현 위치 |
|----|-------------|-----------|----------|
| P4-1 | 에이전트가 OSC 9 알림 전송 | 세션을 `attention` 상태로 전환. 사이드바에 **파란 링 + 미읽음 점**. 알림 텍스트를 미리보기에 표시 | `session-state.ts` |
| P4-2 | OSC 777 (`\x1b]777;notify;title;body\x07`) | P4-1과 동일 처리. title/body를 파싱해 미리보기에 `title: body` | 동일 |
| P4-3 | BEL(`\x07`) 단독 수신 | `attention` 전환. 단 **1초 내 중복 BEL은 1회로 합침** (진행률 표시줄이 BEL을 남발하는 경우 방지) | 동일 |
| P4-4 | 활성 세션이 알림을 보냄 | 사용자가 이미 보고 있으므로 미읽음 표시하지 않는다. 링만 잠깐 표시 | `App.tsx` |
| P4-5 | 알림 후 사용자가 세션 전환 | 해당 세션 미읽음 해제, `attention` → 재평가 | `App.tsx` |
| P4-6 | 출력이 흐르는 중 | `busy`. 마지막 출력 시각을 갱신 | `session-state.ts` |
| P4-7 | 출력이 멎고 프롬프트 패턴으로 끝남 | 400ms 유휴 후 `idle`. PowerShell(`PS ...>`), cmd(`C:\...>`), bash(`$ `/`# `) 패턴 인식 | 동일 |
| P4-8 | 출력이 멎었는데 프롬프트가 아님 | 400ms 유휴 후 `waiting` — 무언가 떠 있는 채로 조용하다. 에이전트가 일을 끝냈거나 확인을 기다리는 전형적 상태 | 동일 |
| P4-9 | 전체화면 TUI 실행 중(vim, less, htop, 에이전트 CLI) | Alternate Screen Buffer 진입(`\x1b[?1049h`) 감지 시 프롬프트 휴리스틱 **비활성화**. 다만 `busy`로 고정하지는 않는다 — 화면이 유휴 문턱(대체 화면에서는 2초 — P4-16)만큼 멎었으면 그리는 일이 끝난 것이므로 `waiting`(추측). 종료(`?1049l`) 시 휴리스틱 재개. claude·codex는 시작하자마자 대체 화면에 들어가 실행 내내 머물기 때문에, 고정하면 P4-14가 이 분기에 가로막혀 무의미해진다 | 동일 |
| P4-10 | 진행률 표시줄이 계속 갱신 | 출력이 계속되므로 자연히 `busy`. 미리보기는 CR로 덮어쓴 최종 라인만 표시 | 동일 |
| P4-11 | 사용자가 타이핑 중 | 입력이 있으면 유휴 타이머 리셋. 타이핑 중 `waiting`으로 튀지 않게 한다 | 동일 |
| P4-12 | 프롬프트 오탐 (출력에 `>`가 포함) | 프롬프트 판정은 **버퍼 마지막 줄**에만 적용하고, 줄 끝이 프롬프트로 끝나야 한다. 중간 일치는 무시 | 동일 |
| P4-13 | 미리보기에 ANSI 코드가 노출 | 미리보기 생성 시 CSI/OSC/SGR 제거, CR 처리, 탭 확장 후 80자로 자름 | 동일 |
| P4-14 | 에이전트 CLI를 띄워둔 채 아무 일도 없음 | 셸 통합(133;C)이 켜져 있어도 `commandRunning`만으로 `busy`를 유지하지 않는다. 400ms 유휴 후 `waiting`. 명령은 몇 시간이고 살아 있으므로 그대로 두면 초록 점이 영영 꺼지지 않고, 정작 일이 돌아갈 때와 구분되지 않아 신호가 죽는다. 줄 전체를 두르는 링도 `attention`에만 준다 | 동일 / `Sidebar.tsx` / `PaneTree.tsx` |
| P4-15 | 타이핑하는 동안 신호등이 깜빡임 | 입력 직후 **200ms 안에 도착한 출력은 에코로 보고 `busy`를 만들지 않는다**. 셸도 에이전트 CLI도 키를 누를 때마다 입력 줄을 통째로 다시 그리는데, 그것까지 "일이 돌아간다"로 세면 한 글자마다 초록으로 올라갔다가 400ms 침묵마다 빨강으로 내려와 타이핑하는 내내 점이 깜빡인다. 내가 친 글자가 화면에 나타난 것은 아무 일도 아니다. 이 판정은 5순위(출력 흐름)에만 적용되므로, 엔터를 눌러 명령을 시작하면 같은 청크에 실린 133;C가 즉시 초록을 켠다 | `session-state.ts` |
| P4-16 | 서브에이전트가 도는 동안 신호등이 깜빡임 | 대체 화면 안에서는 유휴 문턱을 400ms 대신 **2초**로 쓴다. 에이전트 TUI는 일하는 동안 — 특히 서브에이전트에 위임한 동안 — 스피너·경과 시간을 **약 1초 간격**으로만 다시 그리는데, 400ms 문턱은 그 사이 침묵마다 빨강을 켜서 점이 1Hz로 깜빡였다. 깜빡이는 표시는 신호가 아니라 소음이다(P4-15와 같은 교훈). 갱신 주기의 두 배면 프레임 하나를 놓쳐도 초록이 버틴다. 프롬프트 판정이 필요한 일반 화면은 400ms를 유지한다 — 명령이 끝난 것을 빨리 알아야 하는 곳은 그쪽이다. 비용은 TUI가 진짜 멈춘 것을 1.6초 늦게 아는 것뿐이다 | `session-state.ts` |

### 상태 → 시각 표현

왼쪽 점은 **신호등**이다. 세 색이 답하는 질문은 하나뿐이다 — 지금 나를
필요로 하는가. 초록은 일이 돌아가고 있으니 둬도 된다는 뜻, 노랑은 명시적으로
나를 불렀다는 뜻, 빨강은 떠 있는 채로 조용하니 볼 차례라는 뜻이다.

색이 없는 두 상태는 **볼 것이 없다**는 뜻이다. 셸 프롬프트(`idle`)는 아무것도
돌지 않는 빈손이고, 종료(`exited`)는 상태가 아니라 **부재**다. 여기에 색을
주면 "볼 것이 없다"가 "볼 것이 있다"처럼 보인다.

| 상태 | 인디케이터 | 의미 |
|------|-----------|------|
| `busy` | 초록 점 (펄스) | 출력이 흐른다. 일이 돌아가는 중 |
| `attention` | **노란 점 + 링** | 명시적으로 나를 불렀다 (확실) |
| `waiting` | **빨간 점** | 떠 있는 채로 조용하다 — 끝났거나 답을 기다린다 |
| `idle` | 회색 빈 원 | 셸 프롬프트. 아무 명령도 돌지 않는다 |
| `exited` | 회색 사각 + 종료 코드 | 셸이 죽었다 |

---

## P5. 렌더링 · 터미널 UI

| ID | 엣지 케이스 | cvmux 정책 | 구현 위치 |
|----|-------------|-----------|----------|
| P5-1 | 세션 전환 | xterm 인스턴스를 **파괴하지 않는다**. CSS로 표시/숨김만 전환해 스크롤백과 커서 위치를 보존 | `terminal-host.ts` |
| P5-2 | WebGL 컨텍스트 손실 | `webglcontextlost` 이벤트에서 addon을 dispose하고 canvas 렌더러로 폴백. 검은 화면 금지 | 동일 |
| P5-3 | WebGL 초기화 실패 (원격 데스크톱, 구형 GPU) | try/catch로 감싸고 기본 렌더러 사용. 오류를 사용자에게 노출하지 않음 | 동일 |
| P5-4 | DPI 변경 / 모니터 간 창 이동 | `devicePixelRatio` 변화를 감지해 fit 재실행 | 동일 |
| P5-5 | 사이드바 토글 | 애니메이션 종료 후 fit. 애니메이션 중 fit은 잘못된 크기를 만든다 | `App.tsx` |
| P5-6 | 창 최소화 → 복원 | 복원 시 활성 세션만 fit | 동일 |
| P5-7 | 세션 제목이 매우 김 | 사이드바에서 말줄임. 전체 제목은 툴팁 | `Sidebar.tsx` |
| P5-8 | OSC 0/2 (터미널 제목 설정) | 셸이 제목을 설정하면 세션 제목에 반영. 단 사용자가 수동 지정한 제목이 있으면 덮어쓰지 않는다 | `session-state.ts` |
| P5-9 | 터미널이 창을 다 못 채움 | pane은 남은 영역을 **전부** 쓴다. 컨테이너 높이가 터미널이 만들어 낸 높이에 좌우되면 fit이 자기 자신을 재게 되어 처음 열린 크기에서 자라지 못한다 — 창을 키워도 아래가 빈 채로 남는다 | `styles.css` |
| P5-10 | 세션이 쌓일수록 느려짐 | WebGL 컨텍스트는 **보이는 터미널만** 쥔다. 숨은 워크스페이스는 컨텍스트를 놓아주고(인스턴스는 유지, P5-1) 다시 보일 때 새로 잡는다. 브라우저가 살려두는 컨텍스트 수에는 상한이 있어, 세션마다 하나씩 쥐면 오래된 것부터 회수당해 그 터미널이 영영 DOM 렌더러로 떨어진다. 손실 후 재획득은 2회까지 시도한다 | `terminal-host.ts` |

---

## P6. 키보드 · 입력

| ID | 엣지 케이스 | cvmux 정책 | 구현 위치 |
|----|-------------|-----------|----------|
| P6-1 | `Ctrl+C` | 선택한 텍스트가 있으면 **복사**하고 선택을 지운다 — 그래야 이어 누른 Ctrl+C가 인터럽트로 간다. 선택이 없으면 **셸에 전달**한다. Windows Terminal과 같은 손버릇 | `terminal-host.ts` |
| P6-2 | `Ctrl+V` / `Ctrl+Shift+V` | 붙여넣기. xterm에 맡기지 않고 클립보드를 직접 읽는다 (P7-4 / P7-5) | 동일 |
| P6-3 | 앱 단축키가 셸 단축키와 충돌 | 앱 단축키는 셸에서 거의 안 쓰는 조합만 사용: `Ctrl+Shift+N`(새 세션), `Ctrl+Shift+W`(닫기), `Ctrl+Alt+1~8`(전환), `Ctrl+Shift+B`(사이드바). 단순 `Ctrl+N`/`Ctrl+B`는 셸이 쓰므로 **쓰지 않는다** | `App.tsx` |
| P6-4 | 한글 IME 조합 중 | 조합 중(`compositionstart`~`end`)에는 앱 단축키를 처리하지 않는다 | 동일 |
| P6-5 | 터미널에 포커스가 없을 때 타이핑 | 활성 터미널로 포커스를 되돌린다 | `terminal-host.ts` |
| P6-6 | 종료된 세션에서 키 입력 | Enter는 재시작(P1-4), 나머지는 무시 | 동일 |

---

## P7. 클립보드

| ID | 엣지 케이스 | cvmux 정책 |
|----|-------------|-----------|
| P7-1 | 여러 줄 붙여넣기 | bracketed paste mode를 지원한다. 셸이 모드를 켰으면 `\x1b[200~`/`\x1b[201~`로 감싸 자동 실행을 방지 |
| P7-2 | CRLF 붙여넣기 | `\r\n` → `\r`로 정규화. `\n`이 그대로 가면 셸이 두 번 실행한다 |
| P7-3 | 초대용량 붙여넣기 | 1MB 초과 시 확인 프롬프트 |
| P7-4 | `Ctrl+V` | **붙여넣기로 다룬다.** xterm에 맡기면 `Ctrl`+글자 규칙에 따라 `0x16`이 셸로 꽂힌다. 이 조합을 가로채 클립보드를 직접 읽는다 — Windows Terminal과 같은 손버릇 |
| P7-5 | 클립보드에 텍스트가 없음 (이미지만) | 터미널은 이미지를 실어 나를 수 없다. 붙여넣기 키를 흘려보내 안에서 도는 프로그램이 스스로 클립보드를 읽게 한다 — 에이전트 CLI가 스크린샷을 첨부하는 길. 보내는 것은 `0x16`이 아니라 `Alt+V`(`ESC`+`v`)다: Windows에서는 터미널이 Ctrl+V를 가져가는 것이 관례라 CLI들이 이미지 첨부를 Alt+V에 걸어 두었다 (Claude Code는 Windows·WSL에서만 `alt+v`) |

---

## P8. 리소스 한계

| ID | 정책 |
|----|------|
| P8-1 | 세션당 스크롤백 기본 10,000줄 (약 2~5MB). 설정으로 100~200,000줄 사이에서 바꾼다 (`terminal.scrollback`) |
| P8-2 | 상태 판정용 버퍼는 **줄 단위**다 — 현재 줄과 마지막으로 확정된 줄만 남는다. 미완결 이스케이프 시퀀스만 4KB까지 이어 붙이고, 그보다 길어지면 깨진 스트림으로 보고 버린다 |
| P8-3 | 동시 세션 상한 32개 |
| P8-4 | IPC 배치 상한 256KB / 16ms |
| P8-5 | 비활성 세션도 PTY는 계속 돌아간다 (일시정지 금지 — 백그라운드 작업이 목적이므로) |
| P8-6 | main의 재생 버퍼 세션당 256KB. 렌더러 재연결 시 이만큼을 되돌려준다(P9-1). 잘라낼 때는 개행 경계에서 자른다 — 이스케이프 시퀀스 중간에서 자르면 재생 화면이 깨진다 |

---

## P9. IPC · 프로세스 격리

| ID | 엣지 케이스 | cvmux 정책 | 구현 위치 |
|----|-------------|-----------|----------|
| P9-1 | 렌더러 크래시 | main의 PTY는 살아있어야 한다. 렌더러 재로드 시 세션 목록 + 스크롤백을 재전송해 복구 | `ipc.ts` |
| P9-2 | 렌더러가 임의 코드 실행 시도 | `contextIsolation: true`, `nodeIntegration: false`, `sandbox: true`. preload에서 화이트리스트 API만 노출 | `main/index.ts`, `preload/index.ts` |
| P9-3 | main에서 처리되지 않은 예외 | 앱을 죽이지 않고 로그 + 해당 세션만 오류 처리 | `main/index.ts` |
| P9-4 | 외부 링크 클릭 | `shell.openExternal`로 기본 브라우저에 위임. 앱 내 내비게이션 차단 | `main/index.ts` |

---

## P10. 앱 생명주기

| ID | 엣지 케이스 | cvmux 정책 |
|----|-------------|-----------|
| P10-1 | 실행 중 세션이 있는 상태로 **종료** | `busy` 세션이 있으면 확인 대화상자. "N개 세션이 실행 중입니다". 창 닫기는 종료가 아니므로 묻지 않는다(P18-1) |
| P10-2 | 앱 종료 | 모든 PTY의 **프로세스 트리를 정리**하고 종료. 고아 프로세스 금지 |
| P10-3 | 앱 강제 종료(작업 관리자) | 정리 불가는 인정. 다음 실행 시 고아 프로세스 검사는 하지 않는다(범위 밖) |
| P10-4 | 두 번째 인스턴스 실행 | 단일 인스턴스 락. 기존 창을 포커스한다. 트레이에 숨어 있으면 창을 되살린다(P18-5) |

---

## P11. 경로 · 작업 디렉토리

| ID | 엣지 케이스 | cvmux 정책 |
|----|-------------|-----------|
| P11-1 | 새 세션의 기본 cwd | 활성 세션의 cwd를 상속. 없으면 사용자 홈 |
| P11-2 | cwd가 존재하지 않음 | 홈 디렉토리로 폴백하고 세션에 경고 표시 |
| P11-3 | 경로에 공백/한글/유니코드 | 그대로 처리. 사이드바 표시는 홈을 `~`로 축약, 중간을 `…`로 생략 |
| P11-4 | UNC 경로(`\\server\share`) | 지원. PowerShell이 처리하므로 cvmux는 검증하지 않는다 |
| P11-5 | 260자 초과 긴 경로 | 표시는 축약, 전달은 원본 그대로 |

---

## P12. 오류 표면화

| ID | 정책 |
|----|------|
| P12-1 | 사용자가 조치할 수 있는 오류만 UI에 표시한다 (셸 없음, 세션 상한, cwd 없음) |
| P12-2 | 내부 오류는 콘솔 로그로만 남기고 앱 흐름을 막지 않는다 |
| P12-3 | 오류 메시지는 원인 + 다음 행동을 함께 제시한다 |

---

## P13. Git 정보 (2차)

cmux 사이드바는 워크스페이스마다 git 브랜치와 PR 상태를 보여준다. cvmux는
브랜치·변경 여부·ahead/behind를 표시한다. PR 상태는 네트워크 호출과 인증이
필요하므로 이번 범위에서 제외한다.

| ID | 엣지 케이스 | cvmux 정책 | 구현 위치 |
|----|-------------|-----------|----------|
| P13-1 | git 저장소가 아닌 디렉토리 | 아무것도 표시하지 않는다. 오류도 표시하지 않는다 | `git-probe.ts` |
| P13-2 | git이 설치되지 않음 | 최초 1회 확인 후 기능을 조용히 비활성화한다. 매번 실패 로그를 남기지 않는다 | 동일 |
| P13-3 | detached HEAD | 브랜치명 대신 짧은 커밋 해시(7자)를 표시한다 | 동일 |
| P13-4 | rebase/merge/bisect 진행 중 | 해당 상태를 브랜치 옆에 표시한다 (`main (rebase)`) | 동일 |
| P13-5 | 거대 저장소에서 status가 느림 | 3초 타임아웃. 초과하면 이전 결과를 유지하고 다음 주기를 기다린다 | 동일 |
| P13-6 | 폴링이 겹침 | 세션당 동시 실행 1개. 이전 조사가 안 끝났으면 이번 주기를 건너뛴다 | 동일 |
| P13-7 | 셸에서 `cd`로 저장소를 벗어남 | cwd가 바뀌면 즉시 재조사한다. OSC 7을 보내는 셸이면 그 신호를 쓴다 | 동일 |
| P13-8 | 세션이 종료됨 | 조사를 중단한다. 마지막 결과는 그대로 남긴다 | 동일 |
| P13-9 | 브랜치명이 매우 김 | 사이드바에서 말줄임. 전체는 툴팁 | `Sidebar.tsx` |
| P13-10 | worktree / submodule | `git status`가 알아서 처리한다. cvmux는 경로를 해석하지 않는다 | `git-probe.ts` |
| P13-11 | 저장소 이름 | 작업 트리 루트의 폴더명을 쓴다. 사이드바에서 "지금 어느 프로젝트인가"에 답하는 값이라 경로보다 먼저 온다. bare 저장소는 `foo.git`의 꼬리를 뗀다 | 동일 |
| P13-12 | 저장소 **안에서** `cd` | 작업 트리 루트를 함께 싣는다. 루트를 모르면 저장소 안 어디에 서 있는지 계산할 수 없고, 하위 폴더로 옮겨도 사이드바가 꿈쩍하지 않아 추적이 고장 난 것처럼 보인다 | 동일 / `format.ts` |

---

## P14. 리슨 포트 감지 (2차)

`npm run dev`가 `:5173`을 열었다는 사실이 사이드바에 보이는 것이 목적이다.
세션이 띄운 프로세스 트리 전체가 대상이다 — 셸의 손자 프로세스가 여는 경우가 대부분이다.

| ID | 엣지 케이스 | cvmux 정책 | 구현 위치 |
|----|-------------|-----------|----------|
| P14-1 | 포트를 여러 개 리슨 | 오름차순으로 최대 3개 표시, 나머지는 `+N` | `Sidebar.tsx` |
| P14-2 | IPv4/IPv6 이중 바인딩 | 포트 번호로 중복 제거한다 | `port-probe.ts` |
| P14-3 | 프로세스가 죽음 | 다음 주기에 목록에서 사라진다. 즉시성보다 정확성을 택한다 | 동일 |
| P14-4 | 조사 명령이 실패/타임아웃 | 이전 결과를 유지하고 조용히 넘어간다. 5회 연속 실패하면 기능을 끈다 | 동일 |
| P14-5 | 조사 비용 | 전 시스템 스냅샷을 **한 번** 떠서 모든 세션이 나눠 쓴다. 세션마다 조사하지 않는다 | 동일 |
| P14-10 | 프로세스 트리 조회가 비쌈 | 실측: `netstat -ano` 65ms, WMI 프로세스 열거 292ms, `Get-NetTCPConnection` 1288ms. 따라서 **포트 목록은 netstat으로 매 주기**, **프로세스 트리는 LISTEN PID 집합이 바뀌었을 때만** 갱신한다. `Get-NetTCPConnection`은 쓰지 않는다 | 동일 |
| P14-11 | 유휴 상태에서의 폴링 낭비 | 적응적 주기 — `busy` 세션이 있으면 4초, 전부 유휴면 15초. 트리 캐시는 60초가 지나면 무조건 한 번 갱신한다(PID 재사용 대비) | 동일 |
| P14-6 | 세션이 하나도 없음 | 폴링 자체를 멈춘다 | 동일 |
| P14-7 | 폴링이 겹침 | 이전 조사가 진행 중이면 이번 주기를 건너뛴다 | 동일 |
| P14-8 | 프로세스 트리가 깊음 | 부모→자식 맵을 만들어 너비 우선으로 순회한다. 순환은 방문 집합으로 차단 | 동일 |
| P14-9 | 잘 알려진 시스템 포트 | 필터링하지 않는다. 사용자가 띄운 것만 트리에 속하므로 자연히 걸러진다 | 동일 |
| P14-12 | 조사가 타이핑을 멈춤 | 조사는 **워커 스레드**에서만 돈다. `execFile`은 비동기 얼굴을 하지만 프로세스 생성은 부르는 스레드의 이벤트 루프를 붙잡는다 — 실측으로 git 1개 88ms, 서로 다른 cwd 9개 동시 **1755ms**. 메인 프로세스의 이벤트 루프에서 돌리면 그 시간 동안 PTY 입출력이 멎어, 친 키가 한꺼번에 밀려 나온다. 워커로 옮기면 같은 작업에서 메인 정지가 **14ms**로 떨어진다 | `probe-worker.ts` |
| P14-13 | 에이전트가 따로 띄운 셸 | 세션 트리 안에서 따로 도는 셸(powershell/pwsh/cmd/bash/sh/zsh/wsl)을 세어 사이드바에 청록 칩으로 표시한다. claude는 명령을 돌릴 때 자기 셸을 띄우므로, 이것이 "띄워만 뒀다"와 "무언가 붙들고 있다"를 가른다. 세는 규칙 셋 — ① **뿌리는 세지 않는다**(세션 자신의 셸이다. 세면 모든 줄에 칩이 붙어 뜻을 잃는다) ② **셸 안의 셸은 다시 세지 않는다**(Git for Windows는 `bin/bash.exe`가 `usr/bin/bash.exe`를 다시 띄워, 그대로 세면 명령 하나가 둘로 보인다. 실측에서 한 쌍·세 겹으로 관측됐다) ③ **45초 넘게 살아 있어야 센다**. ③의 근거: 에이전트 세션 아래에서 70초 동안 셸이 **169개** 떴다 사라졌고, 도구 호출·git은 0.1~5초, 상태줄 훅은 최대 **12.9초**를 살았으며, 정작 알리고 싶은 상주 셸은 **628초·3632초**였다. 문턱이 낮으면 칩이 몇 초마다 깜빡이는데, 깜빡이는 표시는 신호가 아니라 소음이다(P4-15와 같은 교훈). 처음 20초로 잡았더니 **서브에이전트**가 걸렸다 — 서브에이전트의 빌드·테스트 셸은 수십 초를 살아 문턱을 넘나들었고, 칩이 그 장단에 맞춰 깜빡였다. 45초는 그 무리를 거의 거르면서도 상주 셸에는 여유가 크다. 프로세스 트리는 포트 조사가 이미 뜨는 것을 그대로 쓴다 — 이름과 시작 시각을 더 받는 비용은 실측상 0이고(같은 열거에 칸이 늘 뿐), 이름으로 **거르는** WQL 질의는 오히려 느리다. 다만 셸은 포트를 열지 않아 LISTEN 서명으로는 갱신이 걸리지 않으므로, **일이 돌아가는 동안에는 트리 수명을 20초**로 줄인다. 트리 수명이 정하는 것은 칩의 **내려가는** 쪽뿐이다 — 올라가는 쪽은 45초 문턱이 지배하므로 그 안에 스냅샷이 한 번만 찍히면 되고, 죽은 셸이 20초쯤 더 걸려 있는 것은 깜빡임을 눅이는 완충이다. 색은 신호등의 세 색을 쓰지 않는다 — 그 색들은 "지금 나를 필요로 하는가"에만 답해야 한다 | `port-probe.ts` / `Sidebar.tsx` |

---

## P15. 데스크톱 알림 (2차)

| ID | 엣지 케이스 | cvmux 정책 | 구현 위치 |
|----|-------------|-----------|----------|
| P15-1 | 명시적 알림(OSC 9/777/99/BEL) 수신 | Windows 토스트를 띄운다. 본문은 알림 텍스트, 제목은 세션 이름 | `notifier.ts` |
| P15-2 | 알림을 보낸 세션을 사용자가 보고 있고 창도 포커스됨 | 토스트를 띄우지 않는다. 이미 보고 있는 것을 다시 알릴 이유가 없다 | 동일 |
| P15-3 | 창이 포커스를 잃은 상태 | 활성 세션이라도 토스트를 띄운다 | 동일 |
| P15-4 | 같은 세션이 연속으로 알림 | 5초 안의 중복은 합친다. 마지막 내용만 보여준다 | 동일 |
| P15-5 | 토스트 클릭 | 창을 복원·포커스하고 해당 세션으로 전환한다 | `main/index.ts` |
| P15-6 | 시스템 알림이 꺼져 있음 | `Notification.isSupported()`로 확인하고 조용히 넘어간다. 사이드바 표시는 그대로 동작 | `notifier.ts` |
| P15-7 | 알림 텍스트가 비어 있음 (단독 BEL) | "확인이 필요합니다"로 대체한다 | 동일 |
| P15-8 | Windows 작업 표시줄 그룹화 | `AppUserModelID`를 설정한다. 없으면 Windows가 토스트를 무시한다 | `main/index.ts` |

---

## P16. 세션 영속성 (2차)

cmux는 재시작 시 워크스페이스·작업 디렉토리·스크롤백을 복원한다. cvmux도
같은 것을 복원하되, **복원된 스크롤백은 죽은 텍스트임을 분명히 한다** — 이전
프로세스는 이미 끝났고 새 셸이 그 자리에 있다는 사실을 감추면 안 된다.

| ID | 엣지 케이스 | cvmux 정책 | 구현 위치 |
|----|-------------|-----------|----------|
| P16-1 | 저장 시점 | 세션 생성/종료 시, 그리고 30초 주기. 앱 종료 시 마지막으로 한 번 | `store.ts` |
| P16-2 | 저장 파일이 손상됨 | 파싱 실패 시 무시하고 빈 상태로 시작한다. 손상 파일은 `.bak`으로 보존 | 동일 |
| P16-3 | 저장 중 앱이 죽음 | 임시 파일에 쓰고 원자적으로 rename한다. 반쪽 파일을 남기지 않는다 | 동일 |
| P16-4 | 복원할 cwd가 사라짐 | P11-2와 같은 폴백 — 홈에서 시작하고 경고를 표시한다 | `pty-manager.ts` |
| P16-5 | 스크롤백이 거대함 | 세션당 최대 128KB만 저장한다. 개행 경계에서 자른다 | `store.ts` |
| P16-6 | 복원된 스크롤백과 새 셸의 출력이 섞임 | 복원분 뒤에 구분선을 넣는다 — `── 이전 세션 (복원됨) ──` | `pty-manager.ts` |
| P16-7 | 저장된 세션이 상한을 넘음 | 최근 것부터 `MAX_SESSIONS`까지만 복원한다 | 동일 |
| P16-8 | 사용자가 복원을 원치 않음 | 복원된 세션도 일반 세션처럼 닫으면 그만이다. 다음 저장에서 사라진다 | 동일 |
| P16-9 | 두 인스턴스가 동시에 저장 | 단일 인스턴스 락(P10-4)이 이미 막는다 | — |

---

## P17. 분할 창 (2차)

사이드바 항목이 **워크스페이스**가 되고, 워크스페이스는 하나 이상의 pane을
트리로 담는다. 기존 "세션 = 사이드바 항목"은 "pane 하나짜리 워크스페이스"의
특수한 경우가 된다.

| ID | 엣지 케이스 | cvmux 정책 | 구현 위치 |
|----|-------------|-----------|----------|
| P17-1 | 분할 방향 | **Windows Terminal 관례를 따른다** — `Alt+Shift+=` 오른쪽, `Alt+Shift+-` 아래. 이 앱을 쓸 사람은 이미 그 키를 손에 익혔다. 중첩 분할을 허용하되, 같은 방향으로 이어 나누면 새 pane을 형제로 끼워 넣어 트리가 깊어지지 않게 한다 | `App.tsx`, `layout.ts` |
| P17-2 | 분할 시 새 pane의 cwd | 분할한 pane의 cwd를 물려받는다 (P11-1과 동일 규칙) | `workspace.ts` |
| P17-3 | pane 하나만 남은 상태에서 닫기 | 워크스페이스 전체를 닫는다 | 동일 |
| P17-4 | 중간 노드의 자식이 하나만 남음 | 그 노드를 자식으로 대체해 트리를 평탄화한다. 빈 분할 노드를 남기지 않는다 | 동일 |
| P17-5 | 분할 비율 조정 | 드래그로 조정. 각 pane 최소 크기 120px, 그 아래로는 밀리지 않는다 | `PaneTree.tsx` |
| P17-6 | 분할 직후 크기 | 양쪽 모두 fit을 다시 수행한다. 애니메이션 없이 즉시 | 동일 |
| P17-7 | 워크스페이스 상태 = 여러 pane의 상태 | 가장 주의가 필요한 pane의 상태를 대표로 삼는다. 우선순위 `attention > waiting > busy > idle > exited` | `workspace.ts` |
| P17-8 | 사이드바 미리보기 | 대표 상태를 만든 pane의 미리보기를 보여준다 | 동일 |
| P17-9 | 포커스 | 워크스페이스마다 마지막 포커스 pane을 기억한다. 전환 시 그 pane으로 복귀 | `App.tsx` |
| P17-10 | 세션 상한 | pane 단위로 센다. 전체 pane 수가 `MAX_SESSIONS`를 넘지 않는다 | `pty-manager.ts` |

---

## P18. 백그라운드 상주 (2차)

터미널 워크스페이스를 창 하나에 묶어두면, 창을 닫는 순간 돌던 작업이 전부
죽는다. cvmux는 **창과 세션의 수명을 분리한다** — 창은 세션을 들여다보는
유리창일 뿐이고, 닫는다고 안에 있는 것이 사라지지는 않는다.

대신 지켜야 할 것이 있다. 보이지 않는 곳에서 도는 프로그램은 **끄는 방법이
분명해야 한다**. 트레이 아이콘이 그 책임을 진다.

| ID | 엣지 케이스 | cvmux 정책 | 구현 위치 |
|----|-------------|-----------|----------|
| P18-1 | 창 닫기(X) | 창만 숨긴다. 앱과 세션은 계속 돈다. 아무것도 죽지 않으므로 확인을 묻지 않는다 | `main/index.ts` |
| P18-2 | 숨은 상태에서 알림 도착 | 토스트는 그대로 뜬다. 클릭하면 창을 되살리고 그 세션으로 이동(P15-5와 같은 길) | 동일 |
| P18-3 | 트레이 아이콘 클릭 | 창을 되살린다. 창을 깨우는 경로는 한 곳으로 모은다 — 트레이·토스트·두 번째 인스턴스가 모두 같은 함수를 쓴다 | `tray.ts` |
| P18-4 | 트레이 '종료' | 여기서만 앱이 완전히 끝난다. `busy` 세션이 있으면 **창을 먼저 되살린 뒤** 확인을 묻는다 — 무엇을 죽이는지 보지 못한 채 결정하게 두지 않는다 | `main/index.ts` |
| P18-5 | 숨은 상태에서 바로가기 재실행 | 단일 인스턴스 락이 잡고 창을 되살린다(P10-4의 연장) | 동일 |
| P18-6 | 트레이 아이콘을 만들 수 없음 | 상주를 포기하고 "창 닫기 = 종료"로 되돌린다. 트레이도 없고 창도 닫히지 않아 작업 관리자로만 끌 수 있는 앱을 남기지 않는다 | `tray.ts` |
| P18-7 | 숨은 동안의 출력 | 계속 받아 스크롤백에 쌓는다. 창을 열면 그 사이의 출력이 그대로 있다 | `pty-manager.ts` |

---

## P19. 사이드바가 답하는 것 (2차)

사이드바 한 줄은 **세 가지 질문에만** 답한다: 무엇인가, 어디인가, 지금 어떤가.
그 밖의 것은 넣지 않는다 — 한 줄에 담긴 정보가 많아질수록 정작 상태를 놓친다.

미리보기를 걷어낸 이유가 여기 있다. 마지막 출력 줄을 흘려보내면 에이전트 CLI
안에서는 **사용자가 치고 있는 글자가 그대로 사이드바에 새어 나온다**. 그것은
세션의 상태가 아니라 소음이고, 옆자리에서 보이면 곤란한 내용일 수도 있다.

| ID | 엣지 케이스 | cvmux 정책 | 구현 위치 |
|----|-------------|-----------|----------|
| P19-1 | 한 줄에 담는 것 | 제목 · 저장소 · 상태. 그 아래에 브랜치와 포트가 보조로 붙는다 | `Sidebar.tsx` |
| P19-2 | 마지막 출력 줄 미리보기 | 쓰지 않는다. 자리에는 판정된 상태를 적고, 색은 왼쪽 상태 점과 같은 언어를 쓴다 | 동일 |
| P19-3 | 제목 편집 | 진입점을 **보이게** 둔다 — 줄에 손이 닿으면 나타나는 연필 버튼, 제목 더블클릭, `Ctrl+Shift+E`. 더블클릭만으로는 기능이 있다는 사실 자체가 전달되지 않는다. `Enter` 저장, `Escape` 취소, 바깥을 눌러도 저장한다. 빈 이름은 자동 제목으로 되돌린다는 뜻 | 동일 / `App.tsx` |
| P19-4 | 저장소가 아닌 디렉토리 | 저장소 이름 자리에 축약 경로를 보여준다(P11-3) | 동일 |
| P19-5 | 이름을 치는 중의 앱 단축키 | 편집 중에는 키를 앱으로 넘기지 않는다. `Ctrl+Shift+N`이 글자 대신 새 세션을 만들면 안 된다 | 동일 |
| P19-6 | `idle`과 `waiting`의 문구 | 눈으로 구분되게 쓴다 — "프롬프트 대기"와 "입력 대기(추정)"는 전혀 다른 상황이다 | `format.ts` |
| P19-7 | 저장소 안에서 자리를 옮김 | 저장소 이름 뒤에 루트로부터의 상대 경로를 붙인다(`cvmux\src\core`). 이름만 적으면 `cd`가 화면에 전혀 드러나지 않는다. 깊으면 끝 두 단계만 남기고 줄인다(P11-3) | 동일 |
| P19-8 | 줄 순서 바꾸기 | 위아래로 끌어 옮긴다. 배열 위치가 곧 화면의 자리이자 `Ctrl+Alt+숫자`의 번호이므로 옮기면 번호도 따라 바뀐다. 끌고 있는 줄은 옅게, 놓일 자리에는 줄 **사이**에 선을 긋는다 — 줄 위에 덧그리면 덮어쓴다는 뜻으로 읽힌다. 이름을 고치는 동안에는 끌 수 없다(입력창 안의 글자 선택이 줄 이동으로 새면 안 된다). 범위를 벗어난 자리는 아무 일도 일으키지 않는다 | 동일 / `App.tsx` / `workspace.ts` |

---

## P20. 제어 소켓과 CLI (2차)

cmux는 모든 동작을 CLI와 Unix 도메인 소켓으로 열어 둔다 — 워크스페이스 생성,
pane 분할, 키 입력 전송, 화면 읽기. 그것이 cmux를 "솔루션이 아니라 프리미티브"로
만드는 부분이고, cvmux도 같은 문을 연다. 전송만 **Windows named pipe**로 옮겼다.

이 문의 주된 사용자는 사람이 아니라 **세션 안에서 도는 에이전트**다. 그래서
세션 환경에 소켓 주소와 CLI 경로가 함께 실린다 — 에이전트는 자기가 어디 있는지
몰라도 `cvmux notify`를 부를 수 있어야 한다.

| ID | 엣지 케이스 | cmux 동작 | cvmux 정책 | 구현 위치 |
|----|-------------|-----------|-----------|----------|
| P20-1 | 앱이 꺼져 있을 때의 CLI | `--help` / `--version`은 소켓 없이 동작 | 같다. 소켓 없이 답할 수 있는 것은 소켓 없이 답한다 — 앱이 꺼졌다고 도움말까지 실패하면 도구로 못 쓴다. CLI는 asar 밖에 두고(ELECTRON_RUN_AS_NODE는 asar를 읽지 못한다) 세션 PATH에 그 폴더를 얹는다. 시스템 PATH는 건드리지 않는다 | `cli/index.ts` / `cli/help.ts` / `pty-manager.ts` |
| P20-2 | 앱을 찾는 방법 | 소켓 경로 + `CMUX_SOCKET_PASSWORD` | 파이프 이름은 userData 경로 해시라 설치마다 다르다. 주소와 비밀번호를 `userData/control.json`에 남기고, 종료할 때 지운다 — 꺼진 앱을 가리키는 주소는 거짓말이다 | `control-socket.ts` |
| P20-3 | 세션 안에서 부를 때 | `CMUX_WORKSPACE_ID` 등을 주입 | `CVMUX_SOCKET_PATH` · `CVMUX_SOCKET_PASSWORD`를 세션 환경에 심는다. 마지막에 얹으므로 사용자 환경의 같은 이름을 덮는다 — 지금 도는 앱이 언제나 옳다 | `pty-manager.ts` |
| P20-4 | 대상 지정 | UUID · `workspace:2` · 순번 | 셋 다 받고, 줄여 쓴 id도 접두 일치로 받는다. **여러 개에 걸리면 거부한다** — 하나를 골라 주면 스크립트가 조용히 엉뚱한 것을 닫는다. 인자가 없으면 부르는 쪽의 세션·지금 보고 있는 워크스페이스 | `protocol.ts` / `control.ts` |
| P20-5 | 화면 읽기와 키 입력 | `read-screen` / `send-key` | 화면은 제어 시퀀스를 걷어내고 준다 — 읽는 쪽이 다시 지우게 하지 않는다. `--lines N`은 끝의 빈 줄을 세지 않는다. 키 이름은 Playwright/W3C 표기를 따라 cmux 스크립트를 그대로 옮길 수 있게 한다 | `control-socket.ts` |
| P20-6 | 인증되지 않은 접속 | 비밀번호 필수 | 첫 프레임에 비밀번호가 없거나 틀리면 `unauthorized`. 한 연결에서 한 번만 확인한다. 줄바꿈 없이 1MB를 넘기면 연결을 끊는다 | 동일 |
| P20-7 | 렌더러가 들고 있는 상태 | — | 워크스페이스·pane·알림은 렌더러가 안다. main이 렌더러에 묻고 답을 중계하되, 창이 없거나 5초 안에 답하지 않으면 거부한다 — 소켓 클라이언트를 무한정 기다리게 두지 않는다 | `ipc.ts` / `control.ts` |
| P20-8 | 실패 응답 | 오류 코드 | 예외를 던져 연결을 끊지 않는다. `unauthorized` · `unknown_method` · `invalid_params` · `not_found` · `invalid_state` · `timeout` · `internal_error` 중 하나와 사람이 읽을 수 있는 사유를 함께 준다 | `control-socket.ts` |
| P20-9 | 이벤트 스트림 | `events.stream`, seq로 재개 | 단조증가 `seq`와 4096개 재생 버퍼. **`--after`를 주지 않으면 지금부터다** — 그냥 붙었을 뿐인 구독자에게 과거를 쏟지 않는다. 따라잡으려는 쪽만 마지막 seq를 말한다 | 동일 |

---

## P21. 알림함 · 명령 팔레트 · 찾기 (2차)

사이드바의 파란 링은 **"지금 누가 나를 부르는가"**에 답한다(P4). 그러나
**"자리를 비운 동안 무슨 일이 있었나"**에는 답하지 못한다 — 세션을 한 번
들여다보면 링이 꺼지고, 무엇을 알리려 했는지는 남지 않기 때문이다. cmux가
알림 패널을 따로 둔 이유가 그것이고, cvmux도 같은 것을 둔다.

cmux의 단축키는 ⌘ 조합이지만 여기서는 전부 `Ctrl+Shift`를 쓴다. `Ctrl+P`는
PSReadLine의 이전 기록, `Ctrl+F`는 한 글자 앞으로, `Ctrl+I`는 탭 완성이다 —
셸이 쓰는 키는 가로채지 않는다(P6-1).

| ID | 엣지 케이스 | cmux 동작 | cvmux 정책 | 구현 위치 |
|----|-------------|-----------|-----------|----------|
| P21-1 | 같은 세션이 연달아 부름 | — | 마지막 것만 남긴다. 다섯 줄이 쌓이는 것보다 마지막 한 줄이 지금 상태를 더 정확히 말한다(P15-4와 같은 이유). 단 **이미 읽은 알림에는 합치지 않는다** — 읽은 것을 되살리면 배지가 되살아난다 | `notifications.ts` |
| P21-2 | 목록이 상한을 넘음 | — | **읽은 것부터** 밀어낸다. 오래된 것부터가 아니다 — 가득 찼다고 아직 보지 않은 알림이 사라지면 그건 알림함이 아니라 로그다. 200개 | 동일 |
| P21-3 | 목록 표시 | 알림 패널 | 최신이 위. 미읽음은 왼쪽에 선을 긋고 제목을 강조한다. 시각은 "3분 전"으로 — 절대 시각은 지금 대응해야 하는지를 계산시킨다. 배지는 미읽음이 있을 때만 숫자를 단다(늘 0이 떠 있으면 배지가 신호를 잃는다) | `NotificationPanel.tsx` |
| P21-4 | 읽지 않은 알림으로 이동 | ⌘⇧U | `Ctrl+Shift+U`. 가장 최근 미읽음의 세션으로 옮기고 읽음 처리한다. 소켓에서도 같은 동작(`jump-to-unread`)이며 그때는 창도 함께 깨운다 | `App.tsx` / `control-socket.ts` |
| P21-5 | 세션을 봄 | — | 그 세션의 알림이 전부 읽음이 된다. 사이드바 링과 알림함 배지가 따로 놀면 둘 중 하나는 거짓말이 된다 | `ipc.ts` / `notifications.ts` |
| P21-6 | 명령 팔레트 | ⌃P | `Ctrl+Shift+P`. 한글 제목에 영문 키워드를 함께 달아 영타로도 찾힌다 — `split`으로 "오른쪽으로 분할"이 나오지 않으면 쓸 이유가 없다. 항목마다 단축키를 함께 적어 **팔레트가 단축키를 가르치게** 한다. 같은 점수면 원래 순서를 지킨다(검색할 때마다 자리가 바뀌면 겨냥이 도박이 된다) | `palette.ts` / `CommandPalette.tsx` |
| P21-7 | 읽은 것을 다시 안 읽음으로 | ⌥⌘U | 항목의 점을 눌러 되돌린다. 나중에 보려고 남겨 두는 용도다 | `NotificationPanel.tsx` |
| P21-8 | 앱을 껐다 켬 | 알림 유지 | 알림함도 세션과 함께 저장한다(파일 version 3). 세션 id는 복원 뒤 아무것도 가리키지 않지만 **세션 제목을 함께 저장**하므로 무엇이 불렀는지는 남는다 | `store.ts` |
| P21-9 | 화면 안에서 찾기 | ⌘F | `Alt+F`. 찾은 자리를 스크롤바에도 표시한다 — 10,000줄에서 "몇 개"만 알고 "어디쯤"을 모르면 반쪽이다. 찾는 동안에도 터미널은 계속 돈다 | `terminal-host.ts` / `FindBar.tsx` |
| P21-10 | 디렉토리에서 찾기 | ⌘⇧F (파일 검색) | `Ctrl+Shift+F`로 **모든 세션의 스크롤백**을 찾는다. 이 앱에서 그 자리의 자연스러운 대응물은 파일이 아니라 세션 화면이다 — 실제 질문은 "그 오류를 어느 세션에서 봤더라"이기 때문이다. 결과를 누르면 그 세션의 그 줄로 간다 | `App.tsx` |
| P21-11 | 소켓에서 UI 열기 | `right-sidebar show` | `cvmux panel <notifications\|palette\|find>`. 스크립트가 사람을 대신해 앱을 조작할 수 있어야 한다는 것이 P20의 전제인데, 겹판이 단축키로만 열리면 그 전제에 구멍이 난다 | `control.ts` |

---

## P22. 설정 · 단축키 · 에이전트 이어서 띄우기 (2차)

cmux는 `~/.config/cmux/cmux.json`으로 테마·폰트·사이드바·단축키를 전부 열어 두고,
에이전트 훅으로 세션 id를 적어 두었다가 다시 켤 때 `claude --resume <id>`로
대화를 이어 준다. cvmux도 같은 것을 하되 자리는 Windows 관례를 따른다.

**자동 설치는 Claude Code만 한다.** cmux는 16개 에이전트의 훅을 설치하지만 그
파일 형식은 제각각이고, 확인 없이 짐작해서 쓰면 남의 설정을 망가뜨린다. 대신
`cvmux hooks record --agent <이름>`은 어느 에이전트든 받으므로, 훅 한 줄만
직접 걸면 같은 것이 동작한다.

| ID | 엣지 케이스 | cmux 동작 | cvmux 정책 | 구현 위치 |
|----|-------------|-----------|-----------|----------|
| P22-1 | 설정 파일 자리 | `~/.config/cmux/cmux.json` | `%APPDATA%\cvmux\cvmux.json`이 정본, `~/.config/cvmux/cvmux.json`도 읽는다. 없으면 **주석이 달린 본보기**를 만든다 — 빈 `{}`를 주면 무엇을 쓸 수 있는지 알 길이 없다. 주석과 마지막 쉼표를 허용한다(JSONC) | `config-store.ts` / `config.ts` |
| P22-2 | 값 하나가 잘못됨 | — | 그 값만 기본값으로 되돌리고 나머지는 살린다. `fontSize`에 문자열을 넣었다고 폰트 이름까지 돌아가면 무엇을 고쳐야 하는지 알기 어렵다. 범위를 벗어난 수도 같다 | `config.ts` |
| P22-3 | 오타 난 키 | — | **짚어 준다.** 잘못 적은 키는 아무 일도 일으키지 않고, 아무 일도 일어나지 않는 것이 가장 알아채기 어렵다 — 값을 고쳤는데 화면이 그대로면 사람은 기능이 고장 났다고 읽는다. `cvmux config doctor`가 목록을 준다 | 동일 / `cli/index.ts` |
| P22-4 | 파일을 고쳐 저장 | 설정 다시 읽기 | 저장하면 바로 반영된다. 편집기가 저장 한 번에 이벤트를 여러 번 내므로 묶어서 한 번만 읽는다. 폰트·색·커서·단축키는 그 자리에서, **셸은 다음 세션부터** — 이미 도는 셸을 갈아 끼울 수는 없다 | `config-store.ts` / `terminal-host.ts` |
| P22-5 | 단축키 바꾸기 | 설정에서 전면 변경 | 동작 이름 → 조합 문자열. 키 이름은 **`event.code`**(자리)를 쓴다 — 한글 자판에서 같은 자리를 눌러도 문자가 달라지기 때문이다. 수정자는 정확히 맞아야 한다(`Ctrl+Shift+N`이 `Ctrl+Alt+Shift+N`에도 반응하면 안 된다). 빈 문자열은 그 단축키를 없앤다는 뜻이고, 해석되지 않는 조합 하나가 나머지를 막지 않는다. 동작 이름은 팔레트 항목 id와 같다 — 표를 둘로 나누면 언젠가 한쪽만 손보게 된다 | `keys.ts` / `App.tsx` |
| P22-6 | 앱이 꺼져 있을 때 | — | `config path` · `config init` · `config doctor`는 소켓 없이 답한다. 설정이 잘못돼 앱이 안 뜰 때 쓰는 것이 doctor인데 앱을 요구하면 쓸 수 없다. `config reload`만 앱에 닿는다 | `cli/index.ts` |
| P22-7 | 에이전트 훅 설치 | 16개 에이전트 | Claude Code만 자동 설치한다. 우리가 넣은 항목에 표시를 남겨 **사용자가 직접 건 훅은 건드리지 않고** 우리 것만 지운다. 원본은 `.cvmux-backup`으로 한 번 남긴다. 설정 파일을 읽지 못하면 **덮어쓰지 않는다** — 훅을 못 거는 것보다 남의 설정을 날리는 쪽이 훨씬 나쁘다 | `cli/hooks.ts` |
| P22-8 | 앱을 껐다 켬 | 에이전트 세션 resume | 훅이 적어 둔 대화 id를 저장 시점에 세션 옆으로 옮겨 적는다(세션 id는 복원 때 새로 발급되므로). 다시 켜면 그 셸의 부트스트랩 끝에서 `claude --resume <id>`를 띄운다. 명령은 **우리가 아는 에이전트 표에서만** 만든다 — 저장 파일의 문자열을 그대로 실행하면 파일을 고친 사람이 명령을 심는 자리가 된다. 이어서 띄우기는 복원 때 한 번뿐이다(P1-4의 재시작은 셸을 원한 것이지 대화를 되살려 달라는 뜻이 아니다). `terminal.autoResumeAgentSessions: false`로 끌 수 있다 | `agent-sessions.ts` / `pty-manager.ts` / `store.ts` |
| P22-9 | 훅이 실패함 | — | 훅은 **절대 실패로 끝나지 않는다.** 0이 아닌 코드로 끝나면 에이전트가 그것을 오류로 다룬다. cvmux 세션이 아니거나 앱이 꺼져 있으면 이유만 적고 0으로 나간다 | `cli/index.ts` |

---

## P23. 내장 브라우저 (2차)

cmux는 터미널 옆에 진짜 브라우저를 띄우고, 에이전트가 그것을 조종해 **자기가
고친 웹을 직접 확인**하게 한다. 사람이 대신 눌러 주지 않아도 되는 것이 이
기능의 존재 이유다. cmux는 WKWebView를 쓰고 cvmux는 Electron의
`WebContentsView`를 쓴다.

조작 API는 [agent-browser](https://github.com/vercel-labs/agent-browser)가 세운
방식을 따른다(cmux가 포팅해 온 것과 같다) — **스냅샷이 각 요소에 안정된
이름(`e12`)을 붙이고, 조작은 그 이름을 가리킨다.** 좌표는 에이전트가 쓸 수 없고
CSS 선택자는 페이지가 조금만 바뀌어도 어긋나기 때문이다.

| ID | 엣지 케이스 | cmux 동작 | cvmux 정책 | 구현 위치 |
|----|-------------|-----------|-----------|----------|
| P23-1 | 주소창에 친 것 | — | 스킴이 있으면 그대로, 없으면 판단한다. **`localhost:5173`의 `localhost:`를 스킴으로 오해하지 않는다** — 콜론 뒤가 숫자뿐이면 포트다. 점도 콜론도 없으면 검색어로 본다 | `url.ts` |
| P23-2 | 화면의 자리 | 네이티브 뷰 | 페이지는 DOM이 아니라 창에 얹힌 네이티브 뷰다. 렌더러가 자리표시 div의 사각형을 재서 알려 주고 main이 그대로 얹는다. 보이지 않게 되면(다른 워크스페이스, 접힘, pane 제거) **떼어 낸다** — 떼지 않으면 다른 화면 위에 그대로 떠 있다 | `browser.ts` / `BrowserPane.tsx` |
| P23-3 | 에이전트의 조작 | `browser snapshot/click/fill/…` | 같은 이름의 하위 명령을 둔다. 스냅샷은 **역할이 있는 것과 사람이 읽을 글만** 담는다 — DOM 전체를 주면 토큰이 감당되지 않고 무엇을 눌러야 하는지도 알기 어렵다. 입력은 네이티브 setter + 이벤트로 넣는다(값만 바꾸면 React 같은 화면이 알아채지 못한다) | `browser-agent.ts` |
| P23-4 | 기다리기 | `browser wait` | 제한 시간을 **페이지 안에서** 지킨다. 그 상태가 영영 오지 않아도 호출한 쪽은 반드시 답을 받아야 한다 | 동일 |
| P23-5 | 새 창 요청 | 같은 pane | 팝업을 진짜 창으로 띄우지 않는다 — 터미널 옆에 두려고 만든 기능인데 자리를 벗어난다. 외부 스킴만 기본 브라우저로 넘긴다 | `browser.ts` |
| P23-6 | 페이지가 준 값 | — | 페이지는 신뢰 경계 밖이다. 우리 preload를 얹지 않고, 조작 코드에 값을 심을 때는 **반드시 JSON으로 감싼다** — 선택자나 입력값의 따옴표가 스크립트를 깨뜨리거나 코드가 되어서는 안 된다. 읽을 수 있는 것(`get`)도 고정된 목록이라 페이지가 정하지 못한다 | `browser-agent.ts` |
| P23-7 | 조작이 실패함 | — | `executeJavaScript`는 스크립트가 던지면 우리 메시지를 버리고 "Script failed to execute"만 남긴다. 값으로 감싸 돌려보내고 소켓 쪽에서 다시 오류로 세운다 — 에이전트에게 **"그 요소가 더 이상 없으니 스냅샷을 다시 뜨라"**고 말해 줄 수 있어야 조작 API가 온전하다 | 동일 / `control-socket.ts` |
| P23-8 | 창이 가려짐 | — | 페이지를 계속 그린다(`backgroundThrottling: false`). 이 화면을 실제로 조종하는 것은 뒤에서 도는 에이전트이고, `visibilityState`가 hidden이 되면 타이머와 애니메이션을 멈추는 화면이 많다 | `browser.ts` |
| P23-9 | 대상 지정 | `--surface` | 화면이 하나뿐일 때만 생략할 수 있다. 여럿일 때 아무거나 고르면 스크립트가 조용히 엉뚱한 화면을 조작한다(P20-4와 같은 이유) | `control-socket.ts` |

---

## P24. pane 안의 가로 탭 (2차)

cmux는 pane 하나에 가로 탭(surface)을 여럿 둔다. 화면을 더 나눌 자리가 없는데
세션은 더 필요할 때, 분할 대신 겹쳐 쓰는 자리다. 용어도 cmux를 따른다 —
사이드바 한 줄이 **워크스페이스**, 그 안의 칸이 **pane**, pane 안의 탭이
**surface**다.

| ID | 엣지 케이스 | cmux 동작 | cvmux 정책 | 구현 위치 |
|----|-------------|-----------|-----------|----------|
| P24-1 | 탭 추가 | `new-surface` | 새 탭은 **지금 탭의 바로 오른쪽**에 들어가고 곧바로 활성이 된다. 맨 끝에 붙이면 탭이 많을 때 방금 연 것이 화면 밖에 생긴다. 탭 바는 **둘 이상일 때만** 그린다 — 하나뿐인데 탭 바가 있으면 세로 공간만 잡아먹고 아무것도 알려주지 않는다 | `layout.ts` / `PaneTree.tsx` |
| P24-2 | 탭 닫기 | `close-surface` | 활성 탭을 닫으면 **왼쪽**으로 간다. 오른쪽으로 가면 연달아 닫을 때 커서가 목록 끝까지 밀려가고 방금 보던 자리에서 멀어진다. 마지막 탭이면 pane이, 마지막 pane이면 워크스페이스가 사라진다 | `layout.ts` |
| P24-3 | 다음/이전 탭 | ⌃Tab | `Ctrl+Tab` / `Ctrl+Shift+Tab`. 끝에서 반대쪽으로 돌아간다. **`Ctrl+Tab`은 기본값 중 유일한 Ctrl 단독 조합인데**, 글자 키가 아니고 터미널 호스트가 탭 전환에 쓰는 것이 관례라 셸에 닿지 않는다(P6-1의 예외이자 그 규칙의 취지에는 맞는다) | `layout.ts` / `config.ts` |
| P24-4 | 숨은 탭 | — | 워크스페이스를 닫을 때·상한을 셀 때·저장할 때·찾을 때는 **보이지 않는 탭까지** 센다. 빠뜨리면 탭 뒤의 세션이 남아 프로세스만 살아 있게 된다. 숨은 탭의 미읽음은 탭 이름을 강조해 알린다 | `layout.ts` / `App.tsx` |
| P24-5 | 저장과 복원 | — | 잎이 세션 순번 **목록**을 담는다. 브라우저 화면은 세션이 아니라 순번이 없으므로 저장되지 않고, 활성 자리는 남은 것 기준으로 다시 센다. 탭이 없던 옛 파일(`sessionIndex` 하나짜리)도 그대로 읽는다 | `store.ts` |
| P24-6 | 소켓에서 다루기 | `new-surface` 등 | `list-surfaces` · `new-surface` · `select-surface` · `close-surface`. `surface:2` 참조나 순번, surface id를 받고 **없는 자리를 가리키면 거부한다** — 가장 가까운 탭을 골라 주면 스크립트가 조용히 다른 탭을 닫는다(P20-4와 같은 이유) | `control.ts` / `cli/index.ts` |

---

## P25. 사이드바 메타데이터 · 오른쪽 사이드바 (2차)

여기 담기는 것은 cvmux가 스스로 알아낸 것이 아니라 **에이전트가 적어 둔 것**이다.
상태 pill·진행률·로그·체크리스트 — 전부 소켓으로 들어온다. P4의 휴리스틱은
"지금 무엇이 돌고 있는가"를 짐작할 뿐 **"어디까지 갔는가"**는 알지 못하므로,
그 자리는 에이전트가 직접 채워야 한다.

저장하지 않는다. 진행 중인 일에 대한 기록이라 앱을 껐다 켜면 그 일은 이미
끝났거나 처음부터 다시다 — 남겨 두면 지난 일이 지금인 척한다.

| ID | 엣지 케이스 | cmux 동작 | cvmux 정책 | 구현 위치 |
|----|-------------|-----------|-----------|----------|
| P25-1 | 상태 pill | `set-status` | 같은 이름으로 다시 쓰면 **덮어쓴다** — 에이전트는 단계가 바뀔 때마다 같은 이름으로 쓰므로, 쌓이면 사이드바에 낡은 단계가 남는다. 4개까지, 넘치면 오래된 것부터. 신호등(P4)과 **다른 언어로 그린다**(색 대신 테두리) — 같은 언어를 쓰면 어느 것이 확실한 신호인지 흐려진다 | `workspace-meta.ts` / `Sidebar.tsx` |
| P25-2 | 진행률 | `set-progress` | 0~1. 범위를 벗어나면 잘라 넣는다. **값을 비울 수 있다** — 몇 분의 몇인지 모를 때는 흐르는 막대를 그린다. 0%짜리 막대를 그리면 "시작도 안 했다"로 읽힌다 | 동일 |
| P25-3 | 로그 | `log` | 터미널 출력과 다른 자리다 — 출력은 도구가 뱉은 것이고 이것은 **에이전트가 사람에게 하는 말**이라 스크롤백에 묻히면 안 된다. 500줄, 오래된 것부터 밀려난다 | 동일 / `RightSidebar.tsx` |
| P25-4 | 체크리스트 | `todo` | 항목 50개·한 줄 500자(cmux와 같은 값). **누가 만들었는지(origin)를 남긴다** — 사람이 적은 것을 에이전트가 지우면 안 되는 근거다. 1부터 세는 번호로도 id로도 집을 수 있다 | `workspace-meta.ts` |
| P25-5 | 목록 통째 교체 | `todo set` | **id가 같은 항목은 정체를 지킨다** — 감시 루프가 매 틱마다 전체를 다시 보내도 체크박스가 새로 만들어지지 않아야 한다. 하나라도 잘못되면 **아무것도 바꾸지 않는다**: 반쯤 적용된 체크리스트가 가장 나쁘다 | 동일 |
| P25-6 | 대상 워크스페이스 | `--workspace` | 인자가 없으면 **부르는 쪽의 세션이 있는 워크스페이스**다 — 에이전트는 `CVMUX_SESSION_ID`만 알기 때문이다. 배치는 렌더러가 저장할 때 main으로 넘어오지만, 앱이 막 뜬 직후에는 아직 비어 있으므로 그때는 렌더러에게 한 번 더 묻는다 | `control-socket.ts` |
| P25-7 | 오른쪽 사이드바 | `right-sidebar` | 왼쪽이 "어떤 세션들이 있는가"에 답한다면 이쪽은 **지금 워크스페이스 안에서 무슨 일이 벌어지는가**에 답한다. 로그·할 일·세션·찾기 네 갈래. `Alt+Shift+B`와 소켓 양쪽으로 연다 | `RightSidebar.tsx` / `control.ts` |

---

## P26. 자동 업데이트 (2차)

cmux는 Sparkle로 갱신하고 사용자는 한 번만 내려받으면 된다. cvmux는
`electron-updater`로 **GitHub 릴리스**를 본다 — 저장소가 공개라 별도 서버가
필요 없다.

규칙 하나가 나머지를 정한다. **업데이트가 하던 일을 끊지 않는다.** 터미널
워크스페이스에는 몇 시간짜리 세션이 떠 있고, 알아서 다시 켜는 앱은 그것을
전부 죽인다.

| ID | 엣지 케이스 | cmux 동작 | cvmux 정책 | 구현 위치 |
|----|-------------|-----------|-----------|----------|
| P26-1 | 언제 확인하는가 | Sparkle 주기 확인 | 시작 20초 뒤 한 번, 그 뒤 6시간마다. 뜨자마자 네트워크를 잡으면 시작이 느려 보인다. **개발 빌드에서는 확인하지 않는다** — 갱신할 대상이 없고 오류만 쌓인다 | `updater.ts` |
| P26-2 | 준비됐음을 알리는 법 | — | 타이틀바에 조용한 버튼 하나, 트레이 메뉴에 줄 하나. **대화상자로 막아서지 않는다.** 트레이 줄은 준비됐을 때만 나타난다 — 늘 있는 메뉴 항목은 알림이 되지 못한다 | `App.tsx` / `tray.ts` |
| P26-3 | 언제 설치하는가 | 다음 실행 시 | 내려받기는 자동, **설치는 사람이 부를 때만**. `autoInstallOnAppQuit`은 끈다 — 트레이에서 "끄기"를 누른 사람이 기대한 일이 아니다. 화면에서 누르면 종료와 같은 무게로 묻는다(실행 중 세션 수를 함께 보여준다). 소켓으로 부르면 묻지 않는다 — 스크립트는 이미 결정을 내렸다 | `updater.ts` / `index.ts` |
| P26-4 | 확인 실패 | — | 오프라인이거나 GitHub이 잠깐 답하지 않는 것뿐일 때가 대부분이다. **갱신 실패는 앱의 실패가 아니다** — 사유는 남기되 대화상자로 막아서지 않는다 | `updater.ts` |
| P26-5 | 끄고 싶을 때 | — | `update.enabled: false`. 끈 것은 **자동 확인**이지 기능이 아니므로 `cvmux update check`는 그대로 동작한다 | `config.ts` |
| P26-6 | 배포 | 릴리스 + Sparkle 피드 | 태그를 밀면 CI가 설치 파일과 `latest.yml`을 릴리스에 올린다. 사람이 손으로 파일을 올리는 단계를 남기지 않는다 — 그 단계는 언젠가 잊힌다. 테스트가 깨진 것은 배포하지 않는다 | `.github/workflows/release.yml` |

---

## P27. 다중 창 (2차)

cmux는 창을 여럿 띄우고 창마다 자기 워크스페이스를 갖는다. 모니터가 둘일 때
한쪽에 에이전트를, 다른 쪽에 빌드를 두는 식이다.

규칙 하나가 나머지를 정한다. **창은 유리창이지 그릇이 아니다.** 세션은 앱
전체가 하나로 들고 있고(`PtyManager`), 창은 그중 일부를 들여다보는 창문일
뿐이다 — 창을 닫아도 세션이 죽지 않는 것(P18-1)과 같은 이치다.

| ID | 엣지 케이스 | cmux 동작 | cvmux 정책 | 구현 위치 |
|----|-------------|-----------|-----------|----------|
| P27-1 | 창이 갖는 것 | 창마다 탭 집합 | 창이 갖는 것은 **배치뿐**이다. 세션은 앱 전체가 들고 있으므로 창을 닫아도, 워크스페이스를 다른 창으로 옮겨도 돌던 명령은 그대로 돈다 | `windows.ts` |
| P27-2 | 어느 창이 보낸 요청인가 | — | 창 id는 **main만** 안다. 렌더러에게 알려 주지도, 요청에 실어 보내게 하지도 않는다 — `event.sender`로 되짚으면 되고, 실어 보내게 하면 위조할 수 있는 값이 하나 늘어 다른 창의 배치를 덮어쓸 수 있다 | `windows.ts` / `ipc.ts` |
| P27-3 | 새 창을 여는 법 | ⌘N | `Ctrl+Alt+N`, 팔레트의 "새 창", `cvmux new-window`. `Ctrl+N`은 PSReadLine이 쓰고 `Ctrl+Shift+N`은 이미 새 세션이다(P22-5) | `config.ts` / `App.tsx` / `control-socket.ts` |
| P27-4 | 브라우저 화면은 어느 창에 | — | 화면을 띄운 창에 얹힌다. 워크스페이스가 다른 창으로 건너가면 **자리를 알려 준 창**이 새 주인이다 — 따로 옮기라고 말할 필요가 없고, 옛 창에서 떼지 않으면 양쪽에 하나씩 뜬다 | `browser.ts` / `ipc.ts` |
| P27-5 | 떠도는 세션은 누가 맡나 | — | 배치에 없는 세션은 **첫 창**만 맡는다. 모두가 맡으면 같은 세션이 여러 창에 겹쳐 뜨고, 새로 띄운 창이 남의 세션을 빨아들이는 것도 놀랄 일이다 | `ipc.ts` / `App.tsx` |
| P27-6 | 소켓이 어느 창을 조작하나 | — | `--window`가 짚으면 그 창, 없으면 **마지막으로 포커스된 창**. 첫 창을 쓰면 방금 보고 있던 창이 아니라 처음 만든 창이 움직인다. `--window`는 전역 플래그다 — 명령마다 끼워 넣게 하면 언젠가 하나를 빠뜨린다 | `control-socket.ts` / `cli/client.ts` |
| P27-7 | 워크스페이스를 창 사이로 | 탭 드래그 | `cvmux move-workspace-to-window`. 떼기와 붙이기는 **항상 짝**으로 간다 — 붙이지 못하면 원래 창에 되돌린다. 둘을 따로 부를 수 있게 열어 두지 않는다(워크스페이스를 어디에도 없는 상태로 잃을 수 있다) | `control-socket.ts` / `renderer/lib/control.ts` |
| P27-8 | 창을 닫을 때 | 창 닫기 | 창이 여럿이면 그냥 닫는다. 트레이로 물러나는 것은 **마지막 창**의 이야기다(P18-1) — 둘 중 하나를 닫았는데 숨어 버리면 정리한 줄 알았던 창이 보이지 않는 채로 살아 있다. 소켓으로는 마지막 창을 닫지 못한다 | `index.ts` / `control-socket.ts` |

---

## 구현 추적

각 규칙은 구현 시 해당 코드에 `// P4-9` 형태의 주석을 남긴다.
정책이 구현되지 않은 채 남으면 이 문서의 규칙 ID로 검색해 확인할 수 있다.

```powershell
# 구현되지 않은 정책 찾기
Select-String -Path src\**\*.ts,src\**\*.tsx -Pattern 'P\d+-\d+' | Group-Object { $_.Matches.Value }
```
